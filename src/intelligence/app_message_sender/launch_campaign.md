# Campaign Launched — Message Instructions

The user's campaign has just been activated. This is a follow-up — the user already knows this was in progress.

The output contains `campaign_name` (use this) and `campaign_id` (an internal UUID — NEVER show this to the user).

Write a short message covering:
- The campaign is live — refer to it by `campaign_name` (e.g. "Your campaign '{campaign_name}' is now live"). If `campaign_name` is missing, just say "Your campaign".
- The team is finding more targets automatically
- We've opened the Draper tab so they can track progress

Never display a raw ID or UUID. Two sentences. No greeting, no re-introduction. Upbeat and confident.
